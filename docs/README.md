# APP 配置
[APP 配置](./app_config.md)
- 包含环境变量配置
- 包含支持数据库覆盖的 AppConfig 配置项
- 包含配置优先级说明

# Worker 介绍
[Worker 介绍](./worker_introduction.md)
- 包含 Worker 目录结构、`main.xx` 入口要求、`handler(ctx)` 约定
- 包含 `stdin` / `stdout` 说明
- 包含上传文件、下载文件、返回 HTML 页面的示例

# Worker 开发
[Worker 开发](./development/worker_dev.md)
- 包含 `stderr` 来源与排查方式
- 包含依赖库安装位置与加载规则
- 包含 Worker 调试与开发建议

# Worker SDK
[Worker SDK](./worker_sdk.md)
- 包含 `kv` 与 `db` 的调用方式
- 包含 `namespace` 规则、builder 规则、返回值说明
- 包含 Node / Python 两个 runtime 的示例
